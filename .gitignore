# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Work-item pipeline tooling (transient lock, workshop src/work_items_lock.py)
state/.work-items.lock

# WI-049/084 pipeline runtime (locks + per-run cursor) — runtime, never tracked
# (review-doc precedent; the jsonl ledgers ARE tracked)
state/.escalations.lock
state/.eval.lock
state/spawn-ledger.json

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Session handoff — regenerated each session-end, never tracked
HANDOFF.md
